[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "deiza-mapper"
version = "1.0.0"
description = "Map LLM output to real deliverables: designed PDFs, editable PowerPoint, Word documents and runnable web bundles, rendered HTML-first through Chromium."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Marcos de Aza" }]
keywords = ["llm", "pdf", "pptx", "docx", "html", "chromium", "playwright", "artifacts", "ai", "deiza"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Text Processing :: Markup :: HTML",
  "Topic :: Office/Business",
]
dependencies = [
  "playwright>=1.45",
  "python-docx>=1.1",
  "python-pptx>=1.0",
  "Pillow>=10.0",
  "markdown>=3.6",
  "lxml>=4.9",
  "pypdf>=4.0",
]

[project.optional-dependencies]
math = ["mathml2omml>=0.0.2"]
highlight = ["Pygments>=2.15"]
server = ["Flask>=3.0"]
all = ["mathml2omml>=0.0.2", "Pygments>=2.15", "Flask>=3.0"]
dev = ["pytest>=8", "mathml2omml>=0.0.2", "Pygments>=2.15", "Flask>=3.0", "pymupdf>=1.24"]

[project.urls]
Homepage = "https://github.com/marcosdeaza/deiza-mapper"
Repository = "https://github.com/marcosdeaza/deiza-mapper"
"Deiza" = "https://deiza.org"

[project.scripts]
deiza-map = "deiza_mapper.cli:main"

[tool.setuptools]
packages = ["deiza_mapper"]

[tool.setuptools.package-data]
deiza_mapper = ["static/*.js", "static/*.css"]

[tool.pytest.ini_options]
testpaths = ["tests"]
